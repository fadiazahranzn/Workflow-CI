name: diabetes-xgboost-project

artifact_location: ./mlruns

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path:
        type: path
        default: diabetes_preprocessing/diabetes_preprocessed.csv
    command: >
      python modelling.py --data_path {data_path}
